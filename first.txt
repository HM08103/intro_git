gitの練習をしてきます。
cd xxx 指定ディレクトリに進む
cd.. 一つ上に戻る
ls ファイル一覧

git init    gitリポジトリを作成。.git隠しフォルダが生成される。
git add     ファイル変更をステージングエリア(控室)へ追加する。
git commit  ローカルリポジトリにコミットする。
git push    リモートリポジトリにプッシュする。